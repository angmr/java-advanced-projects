package PointApp;

public class PointXYZ {

    // ορίζει ένα σημείο στον χώρο με συντεταγμένες (x, y, z)
    // default constructor, overloaded constructor, static factory constructor PointXYZ.probableZero()
    // μια μέθοδος convertToString(), μετατρέπει τα Point instances σε Strings για να μπορούμε να εκτυπώνουμε
    // σημεία με println().
}
